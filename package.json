{
  "name": "tv-motel",
  "version": "1.0.0",
  "description": "Plataforma Agregadora de Entretenimento Híbrido",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:front": "pnpm --filter app-front dev",
    "dev:admin": "pnpm --filter admin-panel dev",
    "build:api": "pnpm --filter api build",
    "build:front": "pnpm --filter app-front build",
    "build:admin": "pnpm --filter admin-panel build"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
